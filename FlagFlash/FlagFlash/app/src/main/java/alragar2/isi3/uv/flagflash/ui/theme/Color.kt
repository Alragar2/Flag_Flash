package alragar2.isi3.uv.flagflash.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary palette ─────────────────────────────────────────────────────────
val SkyBlue        = Color(0xFF38BDF8)   // sky-400
val DeepSkyBlue    = Color(0xFF0EA5E9)   // sky-500
val DarkSkyBlue    = Color(0xFF0284C7)   // sky-600
val DeepBlue       = Color(0xFF1E40AF)   // indigo accent

// ── Background / Surface ────────────────────────────────────────────────────
val BgLight        = Color(0xFFE0F6FF)   // very light sky
val Surface        = Color(0xFFFFFFFF)
val SurfaceCard    = Color(0xFFF0FAFF)
val SurfaceOverlay = Color(0xB2FFFFFF)   // glass overlay (70% white)

// ── Text ────────────────────────────────────────────────────────────────────
val TextPrimary    = Color(0xFF0C1F3F)
val TextSecondary  = Color(0xFF3A6A8A)
val TextOnBlue     = Color(0xFFFFFFFF)

// ── Accents ─────────────────────────────────────────────────────────────────
val Gold           = Color(0xFFFBBF24)   // amber-400
val Silver         = Color(0xFFCBD5E1)
val Bronze         = Color(0xFFCD7F32)
val GreenCorrect   = Color(0xFF22C55E)   // green-500
val RedWrong       = Color(0xFFEF4444)   // red-500

// ── Button gradient stops ───────────────────────────────────────────────────
val BtnGradStart   = Color(0xFF38BDF8)
val BtnGradEnd     = Color(0xFF0284C7)
